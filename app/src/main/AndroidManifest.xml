<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cabina10.latinoapp">

    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBjT8DamHvea_7XSm3rAND8uJ_Q8CWbNrg"/>

        <activity
            android:name=".activities.Login"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.StudentsList"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.StudentDetails"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.RegisterForm"
            android:screenOrientation="portrait" />
        <activity
            android:name=".activities.StudentAccount"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.AdministratorUsers" />
        <activity android:name=".activities.MenuActivity" />
        <activity android:name=".activities.Map"></activity>
    </application>

</manifest>